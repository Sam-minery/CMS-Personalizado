# Variables de entorno necesarias para el CMS personalizado
# Copia este archivo a .env.local y ajusta los valores según tu configuración

# URL del servidor (requerido)
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# Secretos de seguridad (requeridos)
PREVIEW_SECRET=your-preview-secret-here
PAYLOAD_SECRET=your-payload-secret-here

# Configuración de la base de datos PostgreSQL (requerido)
DATABASE_URI=postgresql://username:password@localhost:5432/your_database_name

# Configuración de Vercel (opcional, solo si usas Vercel)
# VERCEL_PROJECT_PRODUCTION_URL=your-project.vercel.app

# Configuración de cron (opcional)
# CRON_SECRET=your-cron-secret-here

# Configuración de Next.js (opcional)
# __NEXT_PRIVATE_ORIGIN=http://localhost:3000
